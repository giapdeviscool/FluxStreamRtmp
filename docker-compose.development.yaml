services:
  streamdb:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: glive
      POSTGRES_PASSWORD: glive
      POSTGRES_DB: glive
    ports:
      - "7654:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
